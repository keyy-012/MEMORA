MEMORA HOME V2
- index.html = redesigned premium Home
- assets/romantic-hearts-background.html = APPROVED BACKGROUND MASTER (user-provided, unchanged)
- netlify.toml = baseline security headers

The background master is isolated as a visual layer so it can be reused on future pages.
